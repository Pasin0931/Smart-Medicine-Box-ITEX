void leda(int reed, int greeen, int bluue){
  pixels.setPixelColor(0, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(1, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(2, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(3, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(4, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(5, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(6, pixels.Color(reed,greeen,bluue)); 
  pixels.setPixelColor(7, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(8, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(9, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(10, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(11, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(12, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(13, pixels.Color(reed,greeen,bluue));
  pixels.setPixelColor(14, pixels.Color(reed,greeen,bluue)); 
  pixels.setPixelColor(15, pixels.Color(reed,greeen,bluue));

  // 0,150,0 green
  // 150,0,0 red

  pixels.show();
  //delay(1000);

}